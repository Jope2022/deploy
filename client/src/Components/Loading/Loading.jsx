import React from 'react';
import SearchBar from '../SearchBar/SearchBar';



const Loading = () => {
    return (
        <div>
            <SearchBar />
        </div>
    );
};

export default Loading;
